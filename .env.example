POSTGRES_DB=leaflink
POSTGRES_USER=leaflink
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=localhost

JWT_SECRET=change-this-to-a-random-secret

# --- Multi-machine deployment ---
# If frontend and backend run on different hosts, set these:
# BACKEND_URL=http://<backend-ip>:8000          (used by Nginx in the frontend container)
# ALLOWED_ORIGINS=http://<frontend-ip>          (used by FastAPI CORS, comma-separated)
